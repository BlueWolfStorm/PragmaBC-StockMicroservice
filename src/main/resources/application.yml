server:
    port: 8080
    error:
      include-stacktrace: never

spring:
  application:
        name: StockMicroservice
  datasource:
    url: jdbc:mysql://127.0.0.1:3333/pragma-stock
    username: root
    password: pragma
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update


